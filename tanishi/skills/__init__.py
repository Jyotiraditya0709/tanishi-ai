"""
Tanishi procedural skills — distilled from successful multi-tool sessions.

Inspired by Hermes-style "dream" skill discovery and two-stage memory compaction
(short interaction → reusable procedure). Skills live under ``TANISHI_HOME/skills``
(JSONL index + markdown for humans); the brain injects matching procedures into
context (keyword match) and learns new ones in the background after successful runs.
"""
